# Deterministic fake GitHub PAT used only to verify SecretRedactor detection.
# Fingerprints are commit-scoped: if the fixture region in guardrail.rs is ever edited, the
# modifying commit produces a NEW fingerprint — append it here, never delete the old one.
08543d049e71a7dd9c12be3d897375b95387561d:crates/aikit-core/src/governance/guardrail.rs:github-pat:474
